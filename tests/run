#!/bin/bash

runner=$1

dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
cd $dir

for d in `ls -d . */`
do
	for f in `ls $d/*.py`
	do
		echo "Running: ${runner} $f"
		${runner} $f
	done
done
